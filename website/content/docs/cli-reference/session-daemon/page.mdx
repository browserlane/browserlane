---
title: "bl page"
description: "Manage browser pages (new, close, switch)"
---

{/* AUTO-GENERATED by scripts/gen-reference.mjs — do not edit by hand; run `pnpm gen:reference` */}

Manage browser pages (new, close, switch)

## Usage

```bash
bl page [OPTIONS]
bl page <command>
```

## Options

| Option | Description |
| --- | --- |
| `--headless` | Hide browser window (visible by default) |
| `--json` | Output as JSON |

## Global options

| Option | Description |
| --- | --- |
| `-v, --verbose` | Enable debug logging |

## `bl page new`

Open a new browser page

**Usage**

```bash
bl page new [url] [OPTIONS]
```

**Arguments**

| Argument | Description |
| --- | --- |
| `[url]` | URL to navigate the new page to (omit for a blank page) |

**Options**

| Option | Description |
| --- | --- |
| `--headless` | Hide browser window (visible by default) |
| `--json` | Output as JSON |

**Global options**

| Option | Description |
| --- | --- |
| `-v, --verbose` | Enable debug logging |

**Examples**

```bash
bl page new
# Open a blank new page

bl page new https://example.com
# Open a new page and navigate to URL
```

## `bl page close`

Close a browser page by index (default: current page)

**Usage**

```bash
bl page close [index] [OPTIONS]
```

**Arguments**

| Argument | Description |
| --- | --- |
| `[index]` | Index of the page to close (default: current page) |

**Options**

| Option | Description |
| --- | --- |
| `--headless` | Hide browser window (visible by default) |
| `--json` | Output as JSON |

**Global options**

| Option | Description |
| --- | --- |
| `-v, --verbose` | Enable debug logging |

**Examples**

```bash
bl page close
# Close current page (index 0)

bl page close 1
# Close page at index 1
```

## `bl page switch`

Switch to a browser page by index or URL substring

**Usage**

```bash
bl page switch <target> [OPTIONS]
```

**Arguments**

| Argument | Description |
| --- | --- |
| `&lt;target&gt;` | Page index, or a substring to match against page URLs |

**Options**

| Option | Description |
| --- | --- |
| `--headless` | Hide browser window (visible by default) |
| `--json` | Output as JSON |

**Global options**

| Option | Description |
| --- | --- |
| `-v, --verbose` | Enable debug logging |

**Examples**

```bash
bl page switch 1
# Switch to page at index 1

bl page switch google.com
# Switch to page containing "google.com" in URL
```
