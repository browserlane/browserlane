---
title: "bl uninstall"
description: "Remove bl, its install directory, and the PATH entry"
---

{/* AUTO-GENERATED by scripts/gen-reference.mjs — do not edit by hand; run `pnpm gen:reference` */}

Remove bl, its install directory, and the PATH entry

## Usage

```bash
bl uninstall [OPTIONS]
```

## Options

| Option | Description |
| --- | --- |
| `--purge` | Also remove the Chrome cache and screenshots |
| `-y, --yes` | Skip the confirmation prompt |

## Global options

| Option | Description |
| --- | --- |
| `-v, --verbose` | Enable debug logging |

## Examples

```bash
bl uninstall
# Remove bl (keeps Chrome cache and screenshots)

bl uninstall --purge
# Remove bl and all cached data
```
